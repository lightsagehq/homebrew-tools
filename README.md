# Sapient Homebrew Tap

Homebrew tap for the Sapient CLI.

Install:

```bash
brew install usesapient/tools/sapient
```

Release artifacts are published from the public `usesapient/sapient-cli` repository. Source is maintained privately.